// 상단에 반드시 입력
import React from 'react';

/**
 * 컴포넌트를 만드는 방법
 * 1. 함수
 * 2. 클래스
 */

//컴포넌트는 대문자로 시작한다.
function Hello() {
  return <div>안녕하세요</div>;
}

// Hello라는 컴포넌트를 만든 후 내보낸다는 의미
export default Hello;
